/**
 * 앱 전역 상수 정의
 */

/** 게시글/피드 페이지당 로딩 개수 */
export const PAGE_SIZE = 20;

/** 글·댓글 최대 글자 수 (기획서 기준) */
export const MAX_CONTENT_LENGTH = 300;

export { DEEPLINKS } from './deeplinks';
export { ROUTES, postDetailPath } from './routes';
